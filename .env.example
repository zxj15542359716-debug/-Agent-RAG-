#环境变量模板：复制本文件为 .env 并填写实际值（.env 已被 .gitignore 忽略，不会提交）
#也可不建 .env，直接把以下三项配置到系统环境变量中

#DeepSeek API Key（对话模型 deepseek-v4-pro），缺失时服务拒绝启动
DEEPSEEK_API_KEY=sk-替换为你的Key

#阿里云百炼 DashScope API Key（向量模型 text-embedding-v4），缺失时服务拒绝启动
DASHSCOPE_API_KEY=sk-替换为你的Key

#JWT 登录凭证签名密钥（泄露=任何人可伪造任意用户身份），用下面命令生成随机值：
#  python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=替换为随机生成的长字符串
